'use client';

import { DashboardContent } from '@/components/dashboard/dashboard-content';

/**
 * Homepage
 * Displays the main dashboard with crowd data and temple information
 */
export default function HomePage() {
  return <DashboardContent />;
}